# Core Framework
fastapi==0.115.9
uvicorn[standard]==0.34.2
python-multipart==0.0.20

# LangChain + LLM
langchain==0.3.25
langchain-community==0.3.24
langchain-huggingface==0.1.2
langchain-chroma==0.2.4

# HTML to Markdown conversion (email body cleaning)
markdownify==0.13.1

# ── Ollama provider ──────────────────────────────────────────────────────
# Ollama Python client — used when LLM_PROVIDER=ollama
ollama==0.4.7

# ── HuggingFace provider ──────────────────────────────────────────────────
# Required when LLM_PROVIDER=huggingface
# Also required for embeddings regardless of provider (all-MiniLM-L6-v2)
transformers==4.51.3
torch==2.6.0
accelerate==1.6.0
# NOTE: bitsandbytes is CUDA (NVIDIA) only — not needed on Apple Silicon MPS.
# If you are on a Linux/Windows machine with an NVIDIA GPU, uncomment this:
# bitsandbytes==0.49.2
# sentencepiece==0.2.0
einops==0.8.1

# Vector Store — must be >=1.0.9 to satisfy langchain-chroma 0.2.4
chromadb==1.0.9

# PDF Processing
pypdf==5.4.0
pdfplumber==0.11.6

# Gmail API
google-auth==2.40.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0
google-api-python-client==2.166.0

# Data validation
pydantic==2.11.4
pydantic-settings==2.9.1

# Utilities
python-dotenv==1.1.0
apscheduler==3.11.0
aiofiles==24.1.0
httpx==0.28.1
tenacity==9.1.2
loguru==0.7.3
